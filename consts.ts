export const DEFAULT_ICON_SIZE = '52';

export const IS_TOUCH = window?.matchMedia('(pointer: coarse)')?.matches;

export const MENU_ROUTES = {
  RaceSelection: 'Races',
  ArtifactsIndex: 'Artifacts',
  UltimatesIndex: 'Ultimates',
  MiscIndex: 'Misc',
};
